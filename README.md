# Cptempl
C/C++ project template for Make(and maybe CMake later)
## Description
src/include -> folder for *.h files that will be include in main.c
src/libs -> folder for libraries, it may be *.so, *.a 
Makefile by default have `build`, `debug`, `clean` configs
